# Photo Search Application - Core Dependencies
# This file contains dependencies for the core src/ modules

# Core utilities
tqdm>=4.66.0            # Progress bars for file scanning

# Image and Video Processing
Pillow>=10.0.0          # Image processing and basic EXIF
exifread>=3.0.0         # Detailed EXIF extraction (ALL tags including MakerNote)
ffmpeg-python>=0.2.0    # Video metadata via ffprobe (ALL fields)
python-magic>=0.4.27    # MIME type detection via libmagic
mutagen>=1.47.0         # Audio/video metadata
pillow-heif>=1.1.0      # HEIF/HEIC image support
ImageHash>=4.3.2        # Perceptual hashing for duplicates
PyWavelets>=1.9.0       # Wavelet transforms for hashing

# System Integration
xattr>=1.3.0            # Extended attributes (macOS/Linux)
watchdog>=1.1.0         # File system monitoring
pypdf>=6.0.0            # PDF metadata extraction

# Data Processing
numpy>=1.24.0           # Numerical operations
requests>=2.31.0        # HTTP requests
types-requests>=2.31.0.20241221  # Type stubs for requests (mypy)
psutil>=7.1.3           # System metrics for integrations/tests

# Configuration
python-dotenv>=1.0.0    # Environment variable management

# Note: For server/API dependencies, see server/requirements.txt
# Note: For frontend dependencies, see ui/package.json
