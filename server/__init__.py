"""Photosearch backend server package.

This file exists so tooling (e.g. mypy, editors) treats `server/` as a proper Python
package, enabling relative imports like `from .jobs import ...` in `server/main.py`.
"""
